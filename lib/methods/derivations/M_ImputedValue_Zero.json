{
  "$schema": "../../../model/json_schema/acdc_method.schema.json",
  "schema_version": "0.9.1",
  "$vocabulary": {
    "statistics": "../../model/method/statistics_vocabulary.json",
    "output_classes": "../../model/method/output_class_templates.json",
    "formula_grammar": "../../model/method/formula_grammar.json"
  },
  "conceptId": "M.ImputedValue_Zero",
  "name": "Zero Imputation",
  "label": "Zero Imp",
  "ncitCode": null,
  "description": "Uses a fixed constant (default 0) as the imputed value for all missing observations. Simplest strategy; appropriate when missingness can be interpreted as absence of the measured quantity (e.g., zero cost, zero count).",
  "formula": {
    "notation": "assignment",
    "default_expression": "imputed_value := 0",
    "generic_expression": "imputed_value := <constant>",
    "notes": "No data-dependent computation. The constant is always used regardless of observed values."
  },
  "configurations": [
    {
      "name": "constant",
      "dataType": "decimal",
      "defaultValue": 0,
      "description": "The fixed value to use for imputation. Defaults to 0 but can be set to any value (e.g., 999 for sentinel, or a clinically meaningful default)."
    }
  ],
  "inputs": [],
  "outputs": [
    {
      "output_type": "computed_value",
      "name": "imputed_value",
      "unit_policy": "preserved",
      "dataType": "decimal"
    }
  ]
}
