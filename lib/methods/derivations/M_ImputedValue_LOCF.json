{
  "$schema": "../../../model/json_schema/acdc_method.schema.json",
  "schema_version": "0.9.1",
  "$vocabulary": {
    "statistics": "../../model/method/statistics_vocabulary.json",
    "output_classes": "../../model/method/output_class_templates.json",
    "formula_grammar": "../../model/method/formula_grammar.json"
  },
  "conceptId": "M.ImputedValue_LOCF",
  "name": "Last Observation Carried Forward",
  "label": "LOCF",
  "ncitCode": null,
  "description": "Computes the last non-missing observed value within a partition, ordered by a time variable. The result carries forward the most recent non-missing value and is intended as input to M.Imputation.",
  "formula": {
    "notation": "assignment",
    "default_expression": "imputed_value := LOCF(value) OVER (partition ORDER BY time_order)",
    "generic_expression": "imputed_value := LOCF(<value>) OVER (<partition>+ ORDER BY <time_order>)",
    "notes": "LOCF scans backwards in time_order within each partition and returns the most recent non-missing value. If no prior non-missing value exists, the result is missing."
  },
  "inputs": [
    {
      "name": "value",
      "dataType": "decimal",
      "required": true,
      "cardinality": "single",
      "description": "Source value to carry forward (may contain missing values)"
    },
    {
      "name": "partition",
      "dataType": "code",
      "required": true,
      "cardinality": "multiple",
      "description": "Grouping variables defining the carry-forward scope (typically subject, parameter)"
    },
    {
      "name": "time_order",
      "dataType": "decimal",
      "required": true,
      "cardinality": "single",
      "description": "Variable defining the temporal ordering within partition (e.g., AVISITN, ADY)"
    }
  ],
  "outputs": [
    {
      "output_type": "computed_value",
      "name": "imputed_value",
      "indexed_by": [
        "partition",
        "time_order"
      ],
      "unit_policy": "preserved",
      "dataType": "decimal"
    }
  ]
}
