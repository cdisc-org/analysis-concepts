{
  "$schema": "../../model/method/acdc_methods.schema.json",
  "schema_version": "0.5.0",
  "$vocabulary": {
    "statistics": "../../model/method/statistics_vocabulary.json",
    "output_classes": "../../model/method/output_class_templates.json",
    "formula_grammar": "../../model/method/formula_grammar.json"
  },
  "oid": "M.ImputedValue_Median",
  "name": "Median Imputation",
  "type": "derivation",
  "class": "Data Handling",
  "intent": [
    "MissingDataHandling"
  ],
  "description": "Computes the median of all non-missing observed values within a group as the imputed value. More robust to outliers than mean imputation.",
  "formula": {
    "notation": "assignment",
    "default_expression": "imputed_value := MEDIAN(value) OVER (group_partition)",
    "generic_expression": "imputed_value := MEDIAN(<value>) OVER (<group_partition>+)",
    "notes": "Same partitioning logic as mean imputation. Preferred when the distribution is skewed or when outliers may distort the mean."
  },
  "configurations": [],
  "output_specification": {
    "output_classes": [
      {
        "class": "computed_value",
        "role_name": "imputed_value",
        "dataType": "numeric",
        "description": "The group median of non-missing observed values",
        "statistics": [
          "value"
        ],
        "indexed_by": [
          "group_partition"
        ]
      }
    ]
  },
  "assumptions": [
    "At least one non-missing value exists in the group",
    "Missing data are MCAR or MAR",
    "Median imputation underestimates variance — not suitable when distributional shape must be preserved"
  ],
  "input_roles": [
    {
      "name": "value",
      "dataType": "numeric",
      "required": true,
      "cardinality": "single",
      "description": "Source values from which the median is computed"
    },
    {
      "name": "group_partition",
      "dataType": "string",
      "required": true,
      "cardinality": "multiple",
      "description": "Grouping variables defining the median computation scope"
    }
  ]
}
